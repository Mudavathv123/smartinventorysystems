spring:
  datasource:
    url: jdbc:mysql://localhost:3306/products_db
    username: vinod_mudavath
    password: Mangi@143
  jpa:
    database-platform: org.hibernate.dialect.MySQL8Dialect
    hibernate:  
      ddl-auto: update    # use flyway for schema; use 'update' only for dev
    show-sql: true
    
    properties:
      hibernate:
        format_sql: true

server:
  port: 8080

jwt:
  secret: change_this_to_a_long_random_secret_string_at_least_32_chars
  expiration: 86400000

